# Viva Questions
1. **What is Maven?** A build automation tool used primarily for Java projects.
2. **What is Promtail?** An agent which ships the contents of local logs to a private Loki instance.
3. **How does CI/CD work here?** GitHub Actions automatically runs `mvn clean package` and validates Docker compose.
